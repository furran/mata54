Obs.: Os dados estão em microssegundos, separados por um newline.

O nome dos arquivos de texto seguem o seguinte formato:

resultados_"algoritmo"_"tamanho do alfabeto (opcional)"_"padrão"_"nome do texto".txt

Se o tamanho do padrão excede 100 caracteres, então o campo "padrão" se torna "verylong".